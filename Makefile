
default: list

DEV_PORT=8975

list:
	@$(MAKE) -pRrq -f $(lastword $(MAKEFILE_LIST)) : 2>/dev/null | awk -v RS= -F: '/^# File/,/^# Finished Make data base/ {if ($$1 !~ "^[#.]") {print $$1}}' | sort | egrep -v -e '^[^[:alnum:]]' -e '^$@$$'

serve:
	php -S localhost:$(DEV_PORT)

view:
	open http://localhost:$(DEV_PORT)
